# tests/__init__.py
# Empty on purpose.
#
# Making `tests/` a real Python package (rather than a directory of loose
# .py files) lets us use relative imports inside the test suite, and gives
# pytest a clean module path to display in its verbose output.
